{
  "name": "@verbweaver/shared",
  "version": "1.0.0",
  "description": "Shared constants, types, and utilities for Verbweaver",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "lint": "eslint src --ext .ts,.tsx"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "eslint": "^8.0.0",
    "rimraf": "^4.0.0",
    "typescript": "^4.9.0"
  }
} 